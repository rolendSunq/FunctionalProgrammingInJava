package java8.inaction.chapter14.part01;

public class FirstClassFunction {
	/*
	 * 일급 함수 (First-Class Function)
	 * 함수를 마치 일반값처럼 사용해서 인수로 전달하거나, 결과로 반환받거나,
	 * 자료구조에 저장할 수 있음을 의미한다.
	 * 
	 * JAVA 8이 이전 버전과 구별되는 특징 중 하나가 일급 함수를 지원하는 것이다.
	 * 메서드 레퍼런드 :: 와 lambda 표현식을 지원한다.
	 * Integer.parseInt
	 * Function<String, Integer> strToInt = Integer::parseInt;
	 */
}
